# compiled output
/dist
/node_modules
/build

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Environment variables (chứa API keys nhạy cảm)
.env

# Test and deployment scripts (contains credentials and development utilities - keep locally only)
/test-scripts/

# D? li?u b�i vi?t (c� th? l?n, kh�ng c?n commit)
data/posts.json

# Legacy main folder (contains secrets)
main/

# Project info (contains server credentials and sensitive details)
PROJECT_INFO.md

# Truth Social OAuth token cache (contains access token)
.client_cache

# Telegram user list (contains personal Chat IDs - PII)
data/users.json

# Debug/test output files
response.json

# TypeScript build info (build artifact)
tsconfig.build.tsbuildinfo

# Cache nến Binance (lớn, tái tạo được bằng npm run dataset:build)
data/klines/

# Tập dữ liệu có nhãn (tái tạo được)
data/labeled.json

# Trạng thái hiệu chuẩn runtime — tích lũy trên server, KHÔNG commit bản local để tránh đè
data/calibration.json
data/calibration-verify.json

# Bài bị bỏ qua do hết hạn (runtime state)
data/skipped-analysis.json

# SSH credentials của server — KHÔNG BAO GIỜ commit
server.txt
